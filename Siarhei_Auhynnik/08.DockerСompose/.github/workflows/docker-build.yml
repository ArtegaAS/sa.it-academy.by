name: Docker Build and Push with Telegram

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ secrets.GHCR_USERNAME }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build and push Docker image
        id: build_push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ secrets.GHCR_USERNAME }}/flask-app:latest
            ghcr.io/${{ secrets.GHCR_USERNAME }}/flask-app:${{ github.sha }}

      - name: Send Telegram notification
        if: always()
        run: |
          STATUS=${{ job.status }}
          MESSAGE="Docker build $STATUS
          Repo: ${{ github.repository }}
          Commit: ${{ github.sha }}
          Image: ghcr.io/${{ secrets.GHCR_USERNAME }}/flask-app:${{ github.sha }}
          Branch: ${{ github.ref_name }}"
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d text="$MESSAGE"
